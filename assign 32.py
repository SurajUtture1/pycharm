def lcm(x,y) :
    if x > y:
        z=x
        while(True) :
            if ((z % x == 0 ) and (z % y)
                lcm = z
                break
            z += 1
    return zp
"lcm of 4 and 6 =", lcm(4,6)